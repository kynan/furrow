template(name="friendslist")
  {{#if currentUser}}
  #friends
    ul
      {{#each friends}}
      li
        a(href="{{url}}")
          img(src="{{image.url}}")
          | {{displayName}}
      {{/each}}
  {{/if}}
